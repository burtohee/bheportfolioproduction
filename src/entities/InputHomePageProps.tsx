export interface InputHomePageProps {
    label?: string;
    id: string;
    type?: string;
    className?: string;
    placeholder?: string;
    autoComplete?: string;
    multiline?: boolean;
    name: string;
    validation?: Record<string, any>;
}
